name: Publish common lib
on:
  workflow_dispatch:
  push:
    branches: ['UE5.5']
    paths: ['Signalling/package.json']

jobs:
  call-release:
    uses: ./.github/workflows/publish-npm-package.yml
    with:
      package_path: Common
      tag_prefix: lib-common-
