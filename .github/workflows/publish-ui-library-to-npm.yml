name: Publish frontend-ui lib
on:
  workflow_dispatch:
  push:
    branches: ['UE5.5']
    paths: ['Frontend/ui-library/package.json']

jobs:
  call-release:
    uses: ./.github/workflows/publish-npm-package.yml
    with:
      package_directory: Frontend/ui-library
      tag_prefix: lib-frontend-ui
