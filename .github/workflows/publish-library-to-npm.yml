name: Publish frontend lib
on:
  workflow_dispatch:
  push:
    branches: ['UE5.5']
    paths: ['Frontend/library/package.json']

jobs:
  call-release:
    uses: ./.github/workflows/publish-npm-package.yml
    with:
      package_directory: Frontend/library
      tag_prefix: lib-frontend

